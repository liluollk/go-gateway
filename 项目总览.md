# 自研 AI 网关：项目总览

## 一、背景

### 当前规模

企业 AI 调用量已进入规模化阶段：

| 指标         | 当前规模                            |
| ------------ | ----------------------------------- |
| 日消耗 Token | **60 亿+**（折合成本约 ¥60 万+/天） |
| 活跃使用人数 | **1,600+** 员工                     |
| 日均调用频次 | **200 万+** 次请求                  |
| 接入模型数   | **10+** 个模型                      |
| 供应商数     | **5+** 家                           |

早期战略目标是"快速普及、不限量供应"，现已完成全员 AI 能力覆盖。当前阶段战略重心已从 **"推广普及"** 转向 **"精细化治理与降本增效"**。

### 当前方案（New API）的能力边界

当前使用 **New API**（OneAPI 衍生版本）作为统一接入层，其核心定位是 **"Token 中转站"**——解决多供应商统一接入和基础配额管理，但**不具备企业级 AI 网关所需的治理与优化能力**。

| 问题维度        | 差距分析                                                     |
| --------------- | ------------------------------------------------------------ |
| **成本管控**    | 仅支持总额度限制，缺乏分维度预算预警和熔断机制，无法精细化管控 |
| **Token 优化**  | 仅做请求透传转发，不具备 Prompt 压缩、裁剪等 Token 优化能力  |
| **路由能力**    | 仅支持权重轮询和故障切换，无法根据问题复杂度动态选择模型     |
| **缓存机制**    | 仅支持精确匹配缓存，无法识别语义相似请求，缓存命中率极低     |
| **Prompt 管理** | 无集中模板库和版本管理，各业务线自行维护，质量参差不齐       |

**补充风险：**
- **开源协议风险**：采用 **AGPL-3.0** 许可证，修改源码并内部使用需向所有用户开源，存在合规隐患
- **无 SLA 保障**：稳定性依赖自身运维，无生产级 SLA 承诺
- **高并发瓶颈**：单节点在日消耗 60 亿 Token 场景下存在性能瓶颈

### 自研决策依据

面对 **60 亿 Token/天、1600 人** 的企业级规模，New API 的能力边界已无法支撑当前阶段的战略目标——**精细化成本治理与智能优化**。自研企业级 AI 模型网关是必要且紧迫的决策：

- **成本直接可量化**：通过智能路由将 40% 简单请求降级至低价模型，**每日可节省 ¥20 万+，年化节省超 ¥7,000 万**
- **能力完全可控**：语义缓存、Prompt 管理、智能路由等核心能力可完全按需定制
- **技术栈统一**：全部采用 Go 技术栈，降低维护复杂度

---

## 二、核心目标

建设一个企业统一的 AI 模型调用入口，让业务系统不再直接持有模型供应商密钥，由模型网关统一完成以下核心职责：

| 职责域       | 核心能力                                           |
| ------------ | -------------------------------------------------- |
| **统一接入** | 应用鉴权、模型授权、厂商路由                       |
| **成本治理** | 预算管理、Token 计量、成本统计、限额熔断           |
| **智能优化** | 语义缓存、智能路由、Prompt 优化                    |
| **安全合规** | 供应商密钥加密、模型白名单、操作审计、敏感信息脱敏 |
| **可观测性** | 调用链路追踪、实时监控、运维告警                   |

**一句话目标：** 让平台侧看得清成本、管得住消耗、省得了钱，让业务侧用得简单、用得安全、用得好。

---

## 三、五大能力目标

| # | 目标 | 要解决什么问题 | 预期效果 |
|:--:|------|--------------|----------|
| ① | **统一接入与治理** | 业务线各自对接不同供应商，密钥散落、无统一鉴权 | 一个 API Key 走遍所有模型，供应商密钥对业务侧完全透明 |
| ② | **智能降本** | 简单问题用贵模型、重复请求反复消耗 Token | 智能路由 + 语义缓存，目标节省 **30%-50%** Token 消耗 |
| ③ | **成本精细化管控** | 只有总账单，不知道"谁花了多少钱、花在哪" | 按项目/部门/用户的**多维度成本拆分**，预算预警 + 自动熔断 |
| ④ | **Prompt 治理** | 各业务线各自写 Prompt，质量参差不齐，无法复用 | 集中式 Prompt 模板管理、版本控制、效果追踪 |
| ⑤ | **全链路可观测** | 出问题只能去供应商控制台查，排障效率低 | 调用链路、Token 消耗、延迟、错误码**全链路追踪**，分钟级定位问题 |

---

## 四、预期量化效果

| 维度 | 现状（New API） | 自研后目标 |
|------|----------------|-----------|
| **日 Token 消耗** | 60 亿+ | 降至 **30-40 亿**（节省 30%+） |
| **日成本** | ¥60 万+ | 降至 **¥40 万以内**（年化节省 **¥5,000 万+**） |
| **预算管控** | 无预警，额度耗尽才拒绝 | **80% 预警 + 100% 自动熔断**，成本可预测 |
| **缓存命中率** | <5%（精确匹配） | >30%（语义缓存），大幅减少重复计算 |
| **排障效率** | 需登录各供应商控制台 | **trace_id 一键定位**，5 分钟内完成问题诊断 |
| **模型路由** | 全量请求走固定模型 | **40% 简单请求自动降级**至低价模型，无损用户体验 |

---

## 五、使用场景

### 企业规模

- 企业内部 **1,600+** 员工，覆盖研发、产品、运营、客服、HR、法务等多个业务线
- **500+** 业务应用接入（含内部工具、智能客服、代码助手、数据分析等）

### 流量预估

| 指标 | 当前 | 12 个月后（预估） |
|------|------|-------------------|
| 日消耗 Token | 60 亿+ | 150-200 亿 |
| 日均请求量 | 200 万+ | 500 万+ |
| 峰值 QPS | ~200 | ~500+ |

---

## 六、对业务方的价值

- **研发**：无需关心供应商 API 差异，统一接入，一行配置切换模型
- **产品/运营**：调用量、成本、效果一目了然，预算自主可控
- **管理层**：AI 投入产出比（ROI）可量化，每笔钱花在哪都清楚
- **合规/安全**：供应商密钥不落地，敏感数据可脱敏，操作可审计

---

## 七、MVP 完成情况

| # | 能力 | MVP 状态 | 说明 |
|:--:|------|:--:|------|
| ① | 统一接入与治理 | ✅ 完成 | 鉴权、路由、多供应商 Adapter（OpenAI + Anthropic） |
| ② | 智能降本 | ❌ 未开始 | 智能路由、语义缓存、模型降级均未实现 |
| ③ | 成本精细化管控 | ❌ 未开始 | 无 Token 统计、无预算、无熔断 |
| ④ | Prompt 治理 | ❌ 未开始 | 无模板管理、无版本控制 |
| ⑤ | 全链路可观测 | ⚠️ 部分 | 有 trace_id + 日志，缺 Prometheus 指标和 Dashboard |

MVP 已实现技术选型和技术骨架，具备**生产级中间件链**（鉴权、限流、日志、路由、重试）和 **42 个测试**（单元 + 集成 + 压测）。下一步优先补齐全链路可观测性。

---

**总结：自研 AI 网关的本质，是从"让大家用上 AI"升级为"让 AI 用得聪明"——核心是降本，抓手是智能化治理，目标是让 AI 投入从"成本中心"变成"可量化的效率引擎"。**