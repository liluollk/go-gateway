---

## 方案定位与设计边界

### 背景

#### 1. 当前现状

企业 AI 调用量已进入规模化阶段，现有基础设施面临严峻挑战：

| 指标         | 当前规模                                                     |
| ------------ | ------------------------------------------------------------ |
| 日消耗 Token | **60 亿+**（折合成本约 ¥60 万+/天，以 GPT-4o 约 ¥10/1M tokens 估算） |
| 活跃使用人数 | **1,600+** 员工（覆盖研发、产品、运营、客服等多条业务线）    |
| 接入模型数   | **10+** 个模型（OpenAI、Claude、Gemini、国产模型等）         |
| 供应商数     | **5+** 家                                                    |

早期战略目标是"快速普及、不限量供应"，现已完成全员 AI 能力覆盖。当前阶段战略重心已从 **"推广普及"** 转向 **"精细化治理与降本增效"**。

#### 2. 当前方案的能力边界与差距

当前使用 **New API**（OneAPI 衍生版本）作为统一接入层，其核心定位是 **"Token 中转站"**——解决多供应商统一接入和基础配额管理，但**不具备企业级 AI 网关所需的治理与优化能力**。

经过源码分析、功能验证和生产环境压测，New API 在以下五个核心维度存在能力缺口：

| 问题维度        | New API 实际能力                                             | 企业级需求                                                   | 差距分析                                                     |
| --------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **成本管控**    | 支持按 API Key 设置固定额度（被动扣减型），无预算预警、无自动熔断、无跨应用聚合视图 | 多维度预算（日/月/项目）、80% 阈值预警、超预算自动熔断、成本分账 | 仅能做到"额度耗尽后拒绝"，无法主动预警和精细化治理           |
| **Token 优化**  | 不提供任何 Prompt 层优化（不压缩、不裁剪、不改写），仅做透传转发 | 长上下文自动摘要、历史对话智能裁剪、Query 改写以提升精准度   | **完全缺失**优化手段，大量 Token 被低效消耗                  |
| **路由能力**    | 仅支持**权重轮询**和**故障切换**（Failover），属于规则路由；**不分析请求内容** | 基于问题复杂度动态选择模型（简单→便宜模型，复杂→旗舰模型），成本感知路由 | 无法识别"简单问题"，导致日均约 **40% 的请求**（估算）仍在调用高价模型，造成显著浪费 |
| **缓存机制**    | 支持**精确匹配缓存**（请求 body 完全一致），**不支持语义缓存** | 语义相似问题复用答案（如"年假几天"与"休假政策"命中同一缓存） | 1600 人提问方式各异，精确匹配命中率 **<5%**，缓存形同虚设    |
| **Prompt 管理** | **完全缺失**：无模板库、无版本管理、无 A/B 测试              | 集中式 Prompt 模板管理、版本控制、灰度发布、效果追踪         | 各业务线各自维护 Prompt，质量参差不齐，无法沉淀最佳实践      |

**补充风险：**

- **开源协议风险**：New API 采用 **AGPL-3.0** 许可证，若修改源码并内部使用，需向所有用户开源修改代码，存在合规隐患。
- **高并发瓶颈**：单节点在日消耗 60 亿 Token 场景下存在性能瓶颈，且无官方分布式方案。

#### 3. 自研决策依据

综上，New API 作为"统一接入层"可满足初期需求，但面对 **60 亿 Token/天、1600 人** 的企业级规模，其能力边界已无法支撑当前阶段的战略目标——**精细化成本治理与智能优化**。自研企业级 AI 模型网关是必要且紧迫的决策：

- **成本直接可量化**：假设通过智能路由将 40% 简单请求降级至便宜模型（如 GPT-4o-mini，成本约为 GPT-4o 的 1/20），**每日可节省 ¥20 万+ 成本，年化节省超 ¥7,000 万**。
- **能力完全可控**：语义缓存、Prompt 管理、智能路由等核心能力均可按需定制，不受开源项目功能边界和协议限制。

---

### 目标

建设一个企业统一的 AI 模型调用入口，让业务系统不再直接持有模型供应商密钥，由模型网关统一完成以下核心职责：

| 职责域       | 核心能力                                           | 关键交付物                                   |
| ------------ | -------------------------------------------------- | -------------------------------------------- |
| **统一接入** | 应用鉴权、模型授权、厂商路由                       | 统一 API 接口，业务侧无需感知供应商差异      |
| **成本治理** | 预算管理、Token 计量、成本统计、限额熔断           | 预算看板、分账报表、自动预警                 |
| **智能优化** | 语义缓存、智能路由、Prompt 优化                    | 缓存命中率统计、路由策略配置、Token 节省报表 |
| **安全合规** | 供应商密钥加密、模型白名单、操作审计、敏感信息脱敏 | 审计日志、安全告警、权限管控                 |
| **可观测性** | 调用链路追踪、实时监控、运维告警                   | 调用详情、性能大盘、告警规则                 |

**一句话目标：** 让平台侧看得清成本、管得住消耗、省得了钱，让业务侧用得简单、用得安全、用得好。

---

### 使用场景

#### 1. 企业规模

- 企业内部 **1,600+** 员工，覆盖研发、产品、运营、客服、HR、法务等多个业务线
- **500+** 业务应用接入（含内部工具、智能客服、代码助手、数据分析等）

#### 2. 流量规模

| 指标         | 当前    | 12 个月后（预估） |
| ------------ | ------- | ----------------- |
| 日消耗 Token | 60 亿+  | 150-200 亿        |
| 日均请求量   | 200 万+ | 500 万+           |
| 峰值 QPS     | ~200    | ~500+             |

目标：**10+ 个模型，5+ 家供应商**，通过统一网关提供差异化服务能力。





当前使用 New API 作为 API 网关，存在以下问题：

- **成本不可控**：仅支持总额度限制，缺乏分维度预算预警和熔断机制，无法精细化管控
- **缺乏优化手段**：仅做请求透传转发，不具备 Prompt 压缩、裁剪等 token 优化能力
- **无智能路由**：仅支持权重轮询和故障切换，无法根据问题复杂度动态选择模型
- **缓存机制薄弱**：仅支持精确匹配缓存，无法识别语义相似请求，缓存命中率极低
- **无 Prompt 管理**：无集中模板库和版本管理，各业务线自行维护，质量参差不齐